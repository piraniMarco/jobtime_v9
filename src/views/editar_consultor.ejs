<!DOCTYPE html>
<html>
<head>
    <title>Editar Consultor</title>
    <link rel="stylesheet" type="text/css" href="/styles.css">
</head>

<style>
    body {
        font-family: Arial, sans-serif;
        margin: 20px;
    }
    h1 {
        text-align: center;
    }
    .form-container {
        width: 50%;
        margin: 0 auto;
        padding: 20px;
        border: 1px solid #ccc;
        border-radius: 10px;
        background-color: #f9f9f9;
    }
    .form-group {
        margin-bottom: 15px;
    }
    .form-group label {
        display: block;
        margin-bottom: 5px;
    }
    .form-group input,
    .form-group select {
        width: 100%;
        padding: 8px;
        box-sizing: border-box;
    }
    .form-actions {
        text-align: center;
    }
    .form-actions button {
        padding: 10px 20px;
        margin: 5px;
    }
    table {
        width: 100%;
        border-collapse: collapse;
        margin-top: 20px;
    }
    table, th, td {
        border: 1px solid #ccc;
    }
    th, td {
        padding: 10px;
        text-align: left;
    }
    #success-message {
        display: none;
        width: 50%;
        margin: 20px auto;
        padding: 10px;
        border: 1px solid #4CAF50;
        color: #4CAF50;
        background-color: #dff0d8;
        text-align: center;
        border-radius: 5px;
    }
</style>
<body>
    <div class="container">
        <h1>Editar Consultor</h1>
        <form action="/editar-consultor/<%= consultor.id %>" method="POST">
            <div class="form-group">
                <label for="nome">Nome:</label>
                <input type="text" id="nome" name="nome" value="<%= consultor.nome %>" required>
            </div>
            <div class="form-group">
                <label for="endereco">Endereço:</label>
                <input type="text" id="endereco" name="endereco" value="<%= consultor.endereco %>" required>
            </div>
            <div class="form-group">
                <label for="telefone">Telefone:</label>
                <input type="text" id="telefone" name="telefone" value="<%= consultor.telefone %>" required>
            </div>
            <div class="form-group">
                <label for="email">Email:</label>
                <input type="email" id="email" name="email" value="<%= consultor.email %>" required>
            </div>
            <div class="form-group">
                <label for="login">Login:</label>
                <input type="text" id="login" name="login" value="<%= consultor.login %>" required>
            </div>
            <div class="form-group">
                <label for="senha">Senha:</label>
                <input type="password" id="senha" name="senha">
                <small>Deixe em branco para manter a senha atual.</small>
            </div>
            <div class="form-group">
                <label for="admin">Admin:</label>
                <select id="admin" name="admin" required>
                    <option value="true" <%= consultor.admin ? 'selected' : '' %>>Sim</option>
                    <option value="false" <%= !consultor.admin ? 'selected' : '' %>>Não</option>
                </select>
            </div>
            <button type="submit">Salvar Alterações</button>
        </form>
    </div>
</body>
</html>
